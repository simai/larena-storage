# Smoke

On a disposable SQLite database: create a structure with every supported field type, write records, read them back, filter with each operator and scan 5000 current records; the database file is deleted afterwards.
