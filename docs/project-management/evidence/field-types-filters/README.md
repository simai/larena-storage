# Storage field-types-filters evidence

Storage structures accept `choice`, `choices`, `user` and `datetime` fields, keep `choices` values as JSON arrays and support typed workbench record filter operators with a 5000-record scan limit.
