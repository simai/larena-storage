# Storage MariaDB driver parity

Laravel exposes MariaDB through the `mariadb` driver name. Storage now normalizes that name to the existing MySQL-compatible owned-table shape family while preserving every column and index check.
