{
  "schema": "larena.batch_deviations.v1",
  "batch": "minimal-cms-v1-1-batch-4-storage-roles-publication",
  "wave": "A",
  "package": "larena/storage",
  "launch_record_ref": "docs/project-management/launch-records/minimal-cms-v1-1-batch-4-wave-a-storage-structure-roles.json",
  "deviations": [
    {
      "id": "registry_contribution_by_extend",
      "kind": "wiring_workaround",
      "path": "src/Providers/StorageServiceProvider.php",
      "detail": "larena/core composes DeclaredOperationRegistry from a hard-coded provider list, so a package cannot contribute its operations by binding a tag.",
      "resolution": "Storage extends the resolved registry instance and registers its operations there, which needs no change to core and keeps one catalogue for REST parity and the MCP projection.",
      "follow_up": "core should gain a tagged contributor port; every further package will hit the same wall. Recorded for review.",
      "shape_impact": "none"
    },
    {
      "id": "test_support_path_case",
      "kind": "allowed_file_moved",
      "path": "tests/Support/structure-role-schema.php",
      "detail": "The launch record named tests/support; this package already uses tests/Support, and a lowercase directory would only differ on a case-sensitive filesystem.",
      "resolution": "The helper lives in the existing tests/Support directory.",
      "shape_impact": "none"
    },
    {
      "id": "phpstan_scan_directories",
      "kind": "allowed_file_changed",
      "path": "phpstan.neon.dist",
      "detail": "larena/core and symfony/yaml are declared requirements that composer cannot install into the package vendor tree in this workspace.",
      "resolution": "scanDirectories gains ../core/src and ../../app/vendor/symfony/yaml, beside the ../access/src and ../audit/src entries already there.",
      "shape_impact": "none"
    },
    {
      "id": "audit_yaml_append_position",
      "kind": "authoring_defect_caught",
      "path": "audit.yaml",
      "detail": "The first attempt appended the two role events to the end of the file, which put them after the nonclaims mapping instead of inside the events list — the same class of mistake as the Batch 2 access.yaml defect.",
      "resolution": "The YAML parser rejected it immediately and the entries were inserted under the events key. The Batch 3 descriptor-file validator would also have caught it; this time the parser was faster.",
      "shape_impact": "none"
    },
    {
      "id": "optional_scope_resolver",
      "kind": "implementation_choice",
      "path": "src/Runtime/DatabaseStructureRoleRegistry.php",
      "detail": "The registry accepts a null ScopeRefResolver so a package test can run without the core scope tables.",
      "resolution": "Null means the caller has taken responsibility for the scope, stated in a comment rather than implied. The composed application always binds a resolver, and the smoke proves an unknown scope fails closed there.",
      "shape_impact": "none"
    },
    {
      "id": "traversal_payload_leaked_filtered_ids",
      "kind": "security_correction_during_batch",
      "path": "src/Runtime/DatabaseRecordRelations.php",
      "detail": "The first scoped traversal filtered the rows and returned the surviving records untouched, so a caller who could not read an ancestor still received its id in the materialized path, in the parent link and inside the relation id. The filter hid the row and the payload handed it back.",
      "resolution": "The filter is evaluated for the whole slice first and only the ids it rejected are masked with *, plus the ids the caller supplied. Depth and order survive because they are structural, not identifying. Blanking everything was rejected: it would also hide ancestors the caller may read and make the answer useless for building a tree.",
      "found_by": "writing RelationTraversalScopeTest, which asserted the payload does not contain the hidden id",
      "shape_impact": "none on the frozen table; the traversal page contract is unchanged"
    },
    {
      "id": "relation_identity_rewritten_on_move",
      "kind": "frozen_invariant_upheld",
      "path": "src/Runtime/DatabaseRecordRelations.php",
      "detail": "The freeze defines relation_id as <relation_key>|<from_record_id>|<to_record_id>. A subtree move changes the parent, so leaving the identity alone would leave a primary key naming a parent the row no longer has.",
      "resolution": "A move rewrites the moved record's relation_id along with its path and depth, so the frozen grammar stays true of the row as it is rather than only as it was created. Found in the MySQL smoke, not in a unit test.",
      "shape_impact": "none"
    },
    {
      "id": "path_index_is_driver_specific",
      "kind": "implementation_choice",
      "path": "database/migrations/platform/2026_09_27_000001_create_larena_storage_record_relations_table.php",
      "detail": "A 2048-character column cannot be indexed whole on MySQL with utf8mb4.",
      "resolution": "The migration issues a path(191) prefix index on MySQL and a plain index on SQLite. One Blueprint index would have produced a migration that passes on SQLite and fails on the production driver, which is the same trap as the index-name length limit found in Batch 1.",
      "shape_impact": "none on the frozen index list; only the MySQL prefix length is added"
    },
    {
      "id": "reference_count_excludes_the_tree_edge",
      "kind": "implementation_choice",
      "path": "src/Runtime/DatabaseRecordRelations.php",
      "detail": "explain() first counted every edge leaving the record, so a record with one parent and no references reported one reference.",
      "resolution": "The tree edge is counted separately from references.",
      "shape_impact": "none"
    },
    {
      "id": "localized_flag_kept_out_of_the_storage_schema",
      "kind": "boundary_finding",
      "path": "src/Runtime/DatabaseStorageWorkbench.php",
      "detail": "The first attempt passed the localized flag into the storage schema field list. The workbench integration test failed immediately with storage_schema_field_unknown_key: the schema normalizer has a closed key set of its own, and widening it would mean migrating every stored definition.",
      "resolution": "The flag stays in the workbench structure descriptor. Nothing needs it in the schema: the localized value writer is told which fields are localized by its caller. The reason is written in the code where the next reader will look, and a test asserts the flag is absent from the schema mapping.",
      "follow_up": "If a later batch needs Storage itself to know which fields are localized, that is a schema normalizer change with a definition migration, and it needs its own freeze.",
      "shape_impact": "none on the frozen table; the freeze's field_declaration note said the workbench gains the key, which it does"
    },
    {
      "id": "workbench_test_is_a_source_level_guard",
      "kind": "test_approach",
      "path": "tests/Unit/WorkbenchLocalizedFieldKeyTest.php",
      "detail": "The workbench normalizer is private and calls the schema normalizer, the Property type registry and a database, so exercising it in isolation would mean building the whole workbench graph.",
      "resolution": "The unit test guards the assertion at source level and checks that the behavioural proof — tests/Integration/StorageWorkbenchTest.php, which defines structures with the eight-key shape — is present and still omits the key. That integration test is what caught the storage-schema mistake.",
      "shape_impact": "none"
    },
    {
      "id": "due_schedules_read_added",
      "kind": "contract_addition",
      "path": "src/Contracts/PublicationLifecycle.php",
      "detail": "The sweep proposal needed to answer what a sweep would publish without publishing. Calling sweep with a zero limit was the first attempt and it correctly refused as invalid_limit.",
      "resolution": "dueSchedules() was added to the contract as a read. The sweep now uses it too, so the dry run and the real run read the same rows through the same query.",
      "shape_impact": "none on the frozen tables or operations; the contract gains one read method the freeze did not list"
    },
    {
      "id": "revision_check_injected",
      "kind": "implementation_choice",
      "path": "src/Runtime/DatabasePublicationLifecycle.php",
      "detail": "The freeze requires publishing a revision that does not exist to be refused, but the publication runtime must not reach into the record tables on its own.",
      "resolution": "The check is an injected callable. The composed application binds it against larena_storage_record_versions; a package test leaves it unbound, which means the caller has taken responsibility and is stated in a comment rather than pretended. The composed refusal was observed in the smoke.",
      "shape_impact": "none"
    },
    {
      "id": "smoke_inserted_record_versions_directly",
      "kind": "smoke_limitation",
      "detail": "The publication smoke needed revisions to publish, and the record mutation path is not part of this batch.",
      "resolution": "Three record-version rows were inserted directly in the smoke. Recorded rather than hidden: the publication behaviour is what was under test, and the injected revision check was exercised by the refusal that happened before the rows existed.",
      "shape_impact": "none"
    }
  ],
  "no_deviation_areas": [
    "both table shapes, their keys, index names and identity grammars follow the freeze exactly",
    "the five starter roles and their lifecycles are the frozen set",
    "the five operations, their access codes, audit events, risks and idempotency keys follow the freeze",
    "no existing schema, record or version table changed",
    "the workbench is untouched: the optional localized field key belongs to wave C",
    "no public HTTP, REST or MCP surface added",
    "no change to any package other than larena/storage"
  ],
  "waves": [
    "A",
    "B",
    "C",
    "D"
  ]
}
