# Smoke

The smoke creates the site and organization structures, persists all nodes, resolves parent relations, reads the canonical trees back and scans SQLite table ownership for a forbidden Content surface.
