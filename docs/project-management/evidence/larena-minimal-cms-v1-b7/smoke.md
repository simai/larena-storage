# Smoke

The package smoke creates unrelated typed structures, exercises create/update/delete/restore, preserves a relation value, rejects stale revisions, proves bounded bulk atomicity, and compares deterministic projections from fresh PHP processes over a file-backed SQLite database.
