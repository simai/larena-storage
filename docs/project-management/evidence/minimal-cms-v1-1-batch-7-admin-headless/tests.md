# Tests

RecordOperationExecutionTest: create through the registry and catalog; the gate is asked for the declared scope; update is a compare-and-swap; a stale revision is refused and writes nothing; a non-map value set is refused; an update can be proposed. StorageOperationDeclarationTest: 29 operations, all bound. composer test runs on commit through the quality gate.
